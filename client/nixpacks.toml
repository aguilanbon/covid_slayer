[variables]
NODE_VERSION = "20"

[phases.install]
cmds = [
  "rm -rf node_modules package-lock.json || true",
  "npm install --include=optional --legacy-peer-deps --no-audit"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
